# Hugo 构建输出（根目录，AI 模块的 ai/public 是源码，不能忽略）
/public/
/resources/
/.hugo_build.lock

# Node / Netlify
node_modules/
.netlify/
npm-debug.log*

# 环境变量（密钥不入库，但保留模板）
.env
.env.*
!.env.example

# 系统文件
.DS_Store
Thumbs.db